defaults:
  - base

_target_: gflownet.utils.logger.Logger

tags:
    - gflownet

do:
  online: True        # not offline — you want to see results remotely
  times: True         # logs wall-clock time per step

project_name: "gfn-crop-calibration"